---
name: AI Agent Harness
slug: ai-agent-harness
updated: 2026-09-08
---

# Roadmap — AI Agent Harness

## Now (this milestone)

- [ ] Add MCP onboarding docs for Claude Desktop, Claude Code, and Cursor.
- [ ] Resolve the founder-record drift (full name + school name on the site vs
      canonical.json — see project open questions).

## Next

- [ ] Tokenize the remaining *derived* venture-name forms on the site
      (`<br>` line-breaks, `&#39;` entities) so every occurrence is generated.
- [ ] Add `resources/` to the MCP server exposing each page's copy.

## Later

- [ ] Add `prompts/` to the MCP server (on-brand proposal generation).
- [ ] A `site_stats` tool wired to real analytics once the domain is live.

## Shipped

- `2026-09-08` — Vault scaffold + AGENTS.md + canonical specs created.
- `2026-09-08` — `mcp/server.py` loads `canonical.json` at runtime
      (no hardcoded facts); markdown specs generated by `tools/build_specs.py`.
- `2026-09-08` — the five website pages are generated from `canonical.json`
      via `site/templates/` + `tools/build_site.py`.
