# CLAUDE.md

This repository is governed by `AGENTS.md` at the repository root. Read it first.

The single source of truth is the **Vault** (`vault/`). The website (`*.html`)
and the MCP server (`mcp/server.py`) are projections of the Vault, not the
source. You work on these projects — you do not own them.
